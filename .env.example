# Django
DJANGO_SECRET_KEY=change-me-to-a-random-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Database (optional - falls back to SQLite when not set)
# Example: postgresql://user:password@localhost:5432/orientiq
DATABASE_URL=

# Email (Gmail SMTP)
# For local development without SMTP, leave EMAIL_HOST empty to use the console backend.
# For real email, set EMAIL_HOST=smtp.gmail.com and provide a Google App Password.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=sachwani5123harsh@gmail.com
EMAIL_HOST_PASSWORD=wwdjmgyeilpilrze
DEFAULT_FROM_EMAIL=your-orientiq-gmail@gmail.com

# AI Assistant
AI_PROVIDER=
AI_API_KEY=
AI_MODEL=

# Live Intelligence (Location + Maps + Weather)
LOCATION_PROVIDER=
LOCATION_API_KEY=
MAP_PROVIDER=
MAPS_API_KEY=
WEATHER_PROVIDER=
WEATHER_API_KEY=
