/* ============================================================
   ORIENTIQ — MAIN SCRIPT
   ============================================================ */

document.addEventListener("DOMContentLoaded", function () {
    // Component init and general behaviors live in components.js.
    // This file is a lightweight entry point for page-specific logic.
});